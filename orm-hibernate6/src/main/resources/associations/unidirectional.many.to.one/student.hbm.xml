<?xml version="1.0"?>
<!DOCTYPE hibernate-mapping PUBLIC
        "-//Hibernate/Hibernate Mapping DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-mapping-3.0.dtd">

<hibernate-mapping package="hibernate.framework.apis.associations.unidirectional.many.to.one">
    <class name="Student" schema="public" table="t_student">
        <id name="id" column="id">
            <generator class="native"/>
        </id>
        <property name="name" column="name"/>
        <property name="age" column="age"/>

        <!-- TODO. 使用特定的多对一的标签 => 始终在Many-端维护级联操作
             name: 当前class类型中字段的名称
             class: 映射关系的类型(必须使用类型的全路径)
             column: 自定义的外键列名
             foreign-key: 外键的约束名称，不使用自动生成的=fkkdu5ea5x79eggopv6jknfi8o4
             cascade="save-update" 配置级联操作
             not-null: 在操作时必须提供非空的属性值，需要先存储外键的一端
                       一般不设置成非空，如果有多个外键会无法正确存储 !! -->
        <many-to-one name="grade"
                     class="Grade"
                     column="grade_id"
                     foreign-key="foreign_key"/>

        <!-- TODO. 可以设置多个<many-to-one的关系，注意这里JPA Entity Name的重复问题 -->
        <many-to-one name="grade1"
                     class="hibernate.framework.apis.associations.unidirectional.many.to.one.package1.Grade"
                     column="grade_id_1"
                     foreign-key="foreign_key_1"/>
    </class>
</hibernate-mapping>