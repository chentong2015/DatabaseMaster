TODO. 什么是Fetching ?
The process of grabbing data from the db and making it available to the application.
- Fetching too much data adds overhead in terms of both JDBC communication and ResultSet processing.
- Fetching too little data might cause additional fetching to be needed.

TODO. FetchType: defines whether Hibernate will load data eagerly or lazily 决定是否延迟获取
- static 支持EAGER或者LAZY两种模式(fetch = FetchType.LAZY)
- dynamic

TODO. FetchMode: how Hibernate will fetch the data 决定如何获取(获取的方式)
- FetchMode.SELECT    会执行额外的Select查询语句来获取数据
- FetchMode.JOIN      使用join来查询关联的表
- FetchMode.SUBSELECT 将Main Select查询语句放到Sub Select查询的where in条件中，前提是Main Select查询的结果不止一条数据
- @LazyCollection