Batching在批量持久化和读取数据时，需要控制缓冲池的大小
OutOfMemoryException 避免造成内存不足的异常
Control the size of the first-level cache 释放一级缓存的大小

// flush a batch of inserts and release memory
entityManager.flush();
entityManager.clear();