<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
        https://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context
        http://www.springframework.org/schema/context/spring-context-3.0.xsd">

    <context:property-placeholder system-properties-mode="NEVER"
                                  location="classpath:data-source.properties"/>

    <!-- org.apache.commons.dbcp.basicdatasource 实现了Data Source interface -->
    <bean name="basicDataSource" class="org.apache.commons.dbcp2.BasicDataSource">
        <!-- driver-class-name可以不提供，Spring Boot能够自动从URL连接中判断 -->
        <property name="driverClassName" value="${jdbc.driverClassName}"/>
        <property name="username" value="${jdbc.username}"/>
        <property name="password" value="${jdbc.password}"/>
        <property name="url" value="${jdbc.url}"/>
    </bean>

    <bean name="infoDao" class="JdbcTemplateJdcp.dao.BaseJdbcTemplate">
        <property name="dataSource" ref="basicDataSource"/> <!-- 使用Setter方法来注入属性的值 -->
    </bean>

    <!-- <context:component-scan base-package="com.example.jdbc"/> -->
</beans>